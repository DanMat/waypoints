{
  "name": "@waypoints/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx --yes wrangler dev",
    "build": "tsup src/worker.ts --format esm --clean",
    "deploy": "npx --yes wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "biome check ."
  },
  "dependencies": {
    "hono": "^4.5.0",
    "@danmat/waypoints-core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.0.0"
  }
}
