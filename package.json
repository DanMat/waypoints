{
  "name": "waypoints",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "packageManager": "pnpm@9.10.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "start": "pnpm --filter ./apps/server start",
    "test": "turbo test",
    "lint": "turbo lint",
    "format": "biome check --write .",
    "typecheck": "turbo typecheck",
    "prep:data": "pnpm --filter @waypoints/pipeline prep:data",
    "aggregate": "pnpm --filter @waypoints/pipeline aggregate"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.0",
    "@biomejs/biome": "^2.0.0",
    "@types/node": "^24.0.0"
  }
}
