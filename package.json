{
  "name": "waypoints",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "packageManager": "pnpm@10.34.5",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "deploy": "pnpm --filter @waypoints/web build && pnpm --filter @waypoints/server run deploy",
    "test": "turbo test",
    "lint": "turbo lint",
    "format": "biome check --write .",
    "typecheck": "turbo typecheck",
    "prep:data": "pnpm --filter @waypoints/cli prep:data",
    "aggregate": "tsx packages/cli/src/cli.ts",
    "update:data": "node scripts/update-data.mjs"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.0",
    "@biomejs/biome": "^2.0.0",
    "@types/node": "^24.0.0",
    "tsx": "^4.0.0"
  }
}
